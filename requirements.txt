# Kept for backwards compatibility — the dependency set now lives in pyproject.toml.
# `pip install -r requirements.txt` installs this project in editable mode with
# every extra, which is what the old flat requirements.txt effectively did.
-e .[ingest,serve,query,mcp,dev]
