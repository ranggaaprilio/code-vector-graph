# Plan: Pull from main & reconcile PR #2 changes with current branch

## Current State

- **Branch**: `feature/jina-code-embeding` (3 commits ahead of merge base `28f6b2c`)
- **Local main**: behind `origin/main` by 2 commits (PR #1 merge + PR #2 merge)
- **origin/main** has PR #2 ("Add glossary enrichment and Field graph nodes") merged, which adds:
  - `src/glossary.py` (new) — glossary extraction & graph enrichment
  - `src/graph_schema.py` (modified) — added `Field` node label, `HAS_GLOSSARY` rel type, `GlossaryEntry` schema
  - `src/graph_extractor.py` (modified) — Field extraction, relationship deduplication, removed INHERITS/CALLS edges
  - `src/graph_store.py` (modified) — validation, database_ param, write count tracking
  - `src/store.py` (modified) — added glossary payload fields (`term`, `kind`, `source`, `symbol_id`, etc.), `graph_nodes`, `graph_relationships`
  - `main.py` (modified) — integrated glossary pipeline (load_manual_glossary, extract_comment_glossary, build_glossary_graph)
  - `requirements.txt` (modified) — added `PyYAML>=6.0`
  - `tests/test_integration.py`, `tests/test_store.py` (modified) — glossary-related test additions
  - `tests/test_glossary.py` (new), `tests/test_reinit_neo4j_from_qdrant.py` (new)
  - Scripts & IDE config files

- **Feature branch** has uncommitted changes in 12 files (Jina model migration, config changes)

## Overlapping Files (Conflicts Expected)

Both branches modify since `28f6b2c`:
| File | main (PR #2) | feature (Jina) | Conflict Risk |
|------|-------------|----------------|---------------|
| `main.py` | Glossary integration, formatting | Jina embedding, formatting changes | **HIGH** |
| `requirements.txt` | Added PyYAML | Upgraded torch/transformers | **MEDIUM** |
| `src/store.py` | Glossary payload fields + graph_nodes/graph_relationships | Jina dimension docstring changes + point_id override | **MEDIUM** |
| `tests/test_integration.py` | Added glossary_file arg assertions | Jina config changes | **LOW-MEDIUM** |
| `tests/test_store.py` | Added glossary payload + graph payload tests | Jina dimension changes | **LOW-MEDIUM** |

## Steps

### 1. Stash uncommitted changes
```bash
git stash push -m "jina-wip-changes"
```
All 12 modified files have working changes that must be preserved.

### 2. Update local main branch
```bash
git checkout main
git pull origin main
```

### 3. Merge main into feature branch
```bash
git checkout feature/jina-code-embeding
git merge main
```
Resolve any conflicts manually. Key resolution strategy for each overlapping file:

- **`main.py`**: Keep Jina embedding changes from feature branch AND integrate glossary pipeline additions from main. This means:
  - Keep the Jina-related import changes (e.g., `create_embedder`, config imports)
  - Add glossary imports (`from src.glossary import ...`)
  - Add `glossary_entries` stat counter
  - Add `manual_glossary_entries` initialization and `load_manual_glossary()` call
  - Add `batch_graph_data`, `batch_code_chunks_created` variables
  - Add graph extraction + glossary enrichment in the file processing loop
  - Keep the Jina-specific formatting/style changes (f-string logging improvements)

- **`requirements.txt`**: Merge both — keep PyYAML from main AND upgraded torch/transformers from feature

- **`src/store.py`**: Keep both sets of changes — Jina dimension docstrings AND glossary payload fields + graph fields + point_id override. These are in different parts of the file and should merge cleanly or be easy to resolve.

- **`tests/test_integration.py`**: Add `glossary_file` assertions from main AND keep Jina-specific config changes from feature

- **`tests/test_store.py`**: Add glossary payload test + graph payload test from main AND keep Jina dimension changes from feature

### 4. Pop stash and resolve
```bash
git stash pop
```
Re-apply the 12 modified file changes on top of the merge. There may be conflicts here too, since the stash changes touch `main.py`, `src/store.py`, `tests/test_integration.py`, `tests/test_store.py` which are also being merged.

Resolution strategy is the same: keep all Jina model changes AND all glossary additions.

### 5. Run tests to verify compatibility
```bash
python -m pytest tests/ -v
```
Fix any test failures arising from the merge.

### 6. Key compatibility concerns to verify
- `src/config.py` — Jina provider config must coexist with any config changes from PR #2
- `src/embedder.py` — Jina embedder must still work; PR #2 didn't change this file
- `src/glossary.py` (new from PR #2) — needs `PyYAML` in requirements (already handled in merge)
- `src/graph_store.py` — PR #2 added validation and `database_` param; feature branch uncommitted changes to `src/store.py` need to be compatible
- `src/graph_extractor.py` — PR #2 added Field extraction and removed INHERITS/CALLS; Jina branch didn't touch this file, so no conflict

### Summary
The merge is feasible. The overlapping files have additive changes on both sides — glossary features from main and Jina model migration from the feature branch. Both sets of changes are orthogonal (different features) so they should coexist once conflicts are resolved by keeping both.